#----------Installation----------#
docker_install: false
docker_install_list: [ docker-ce ]
#-Custom repository
docker_repository: true
docker_repository_url: "https://download.docker.com/linux"
docker_repository_gpg_key: "https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg"
docker_repository_deb: "deb [arch=amd64] {{ docker_repository_url }}/{{ ansible_distribution | lower }} {{ ansible_distribution_release }} stable"
